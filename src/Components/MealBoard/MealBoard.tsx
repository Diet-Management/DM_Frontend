import * as S from "./Style";

export default function RecommendMeal(children:any) {
    return(
        <S.Layout>
            <S.TitleBox>

            </S.TitleBox>
            <S.MealBox>
                
            </S.MealBox>
        </S.Layout>
    );
}